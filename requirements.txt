# =============================================
# RAG Document Cataloger - Python Dependencies
# =============================================

# --- Web Framework ---
fastapi>=0.100.0               # Async web framework for API endpoints
uvicorn>=0.22.0                # ASGI server to run FastAPI
pydantic>=2.0.0                # Data validation and settings management
python-multipart>=0.0.6        # File upload support for FastAPI

# --- Document Extraction ---
pdfplumber>=0.10.0             # PDF text and table extraction
pillow>=10.0.0                 # Image processing for table snapshots
python-docx>=1.1.0             # DOCX file reading
pandas>=2.0.0                  # CSV/data processing

# --- LangChain & Embeddings ---
langchain>=0.1.0               # LLM framework core
langchain-community>=0.0.12    # Community integrations (FAISS vectorstore)
langchain-huggingface>=0.1.0   # HuggingFace embeddings integration
langchain-core>=0.1.7          # Core abstractions (Document, etc.)
langchain-text-splitters>=0.2.0 # Semantic text chunking

# --- ML / Embeddings / Torch ---
sentence-transformers>=5.0.0   # Embedding model (all-MiniLM-L6-v2)
transformers>=4.41.0           # HuggingFace model loading
torch>=2.0.0                   # PyTorch backend for models
scikit-learn>=1.3.0            # ML utilities
numpy>=1.23.0                  # Numerical computing

# --- Vector Store ---
faiss-cpu>=1.7.4               # FAISS vector similarity search

# --- AI Generation (LLM APIs) ---
groq>=0.4.0                    # Groq API client for LLM generation (Llama 3.3 70B)
google-generativeai>=0.8.0     # Google Gemini API client (gemini-2.0-flash)

# --- Database ---
python-dotenv>=1.0.0           # .env file loading

# --- Authentication ---
python-jose[cryptography]>=3.3.0  # JWT token creation/validation
passlib[bcrypt]>=1.7.4         # Password hashing with bcrypt
email-validator>=2.0.0         # Email format validation

# --- OCR (optional - set ENABLE_OCR=true in .env) ---
# pytesseract>=0.3.10          # Tesseract OCR wrapper
# pdf2image>=1.16.0            # Convert PDF pages to images for OCR

# --- Testing ---
pytest>=7.0.0                  # Test framework
pytest-asyncio>=0.21.0         # Async test support
httpx>=0.24.0                  # HTTP client for API tests